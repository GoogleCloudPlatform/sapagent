{
  "name": "Knowledgebase",
  "id": "knowledgebase",
  "labels": [
    "Internal"
  ],
  "queries": [
    {
      "name": "q_hana_version",
      "sql": "SELECT VERSION FROM SYS.M_DATABASE",
      "columns": ["version"]
    },
    {
      "name": "q_hana_nodes",
      "sql": "SELECT DISTINCT(HOST) AS hosts FROM M_HOST_INFORMATION",
      "columns": ["hosts"]
    },
    {
      "name": "q_largest_schema",
      "sql": "SELECT SCHEMA_NAME as schema, SUM(DISK_SIZE) / 1024 / 1024 / 1024 AS size FROM M_TABLE_PERSISTENCE_STATISTICS GROUP  BY SCHEMA_NAME ORDER BY SIZE DESC LIMIT 1",
      "columns": ["schema", "size"]
    }
  ]
}
